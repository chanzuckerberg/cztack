sudo: required
dist: trusty
language: generic
services:
  - docker
install:
  - wget -o terraform.zip https://releases.hashicorp.com/terraform/0.11.7/terraform_0.11.7_linux_amd64.zip
  - unzip terraform.zip

jobs:
  include:
    - stage: check
      script: make lint